---
/**
 * Google Tag Manager head snippet. GA4 lives inside the GTM container; never a separate GA4_ID.
 * Defaults to the production container so preview deploys carry GTM without extra env wiring.
 */
const gtmId = import.meta.env.GTM_ID || import.meta.env.PUBLIC_GTM_ID || 'GTM-W5NHK72H';
---

{gtmId && (
  <script is:inline define:vars={{ gtmId }}>
    (function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
    new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
    j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
    'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
    })(window,document,'script','dataLayer', gtmId);
  </script>
)}
